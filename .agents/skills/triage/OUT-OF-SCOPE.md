# Out of Scope Rules

Place any files documenting out-of-scope boundaries inside `.out-of-scope/` in your repository.
The triage agent will scan this directory to instantly close tickets matching these boundaries as `wontfix`.
